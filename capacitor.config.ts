import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'responsi2-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
